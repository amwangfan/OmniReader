name: Server tests

on:
  push:
    branches:
      - "agent/**"
  pull_request:
    paths:
      - "server/**"
      - ".github/workflows/server-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum
      - name: Run server tests
        run: go test ./...

  conversion-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum
      - name: Install Calibre CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends calibre
      - name: Convert TXT to EPUB with Calibre
        run: go test ./internal/books -run TestInstalledCalibreConvertsTXTToEPUB -count=1 -v
